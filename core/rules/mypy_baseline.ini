# WARDEN Core Mypy Baseline Configuration
# Prevents target projects from evading type checks via local ignore_errors / lax configs.

[mypy]
ignore_missing_imports = True
check_untyped_defs = True
no_error_summary = True
show_error_codes = True
