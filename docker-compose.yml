version: '3.8'

services:
  warden:
    build: .
    container_name: warden_service
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
